@Library('roboshop') _

pipeline{
    agent any

    stages{
        stage('Shared Lib Demo'){
            steps{
                log.info 'Starting'
                log.warning'Nothing to do,its warning only'
            }
        }
    }//stages
}//pipeline